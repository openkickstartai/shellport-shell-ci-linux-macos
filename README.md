# shellport-shell-ci-linux-macos
每个在 macOS 上开发、Linux 上部署的团队都踩过这个坑：`sed -i ''` vs `sed -i`、`grep -P` 在 BSD 上不存在、`readlink -f` 在 macOS 上要装 coreutils、`date -d` vs `date -j`、`mktemp` 参数不一致……这些差异藏在 shell 脚本、Makefile、Dockerfile、CI YAML 和 README 的代码块里，直到某天凌晨 ...
